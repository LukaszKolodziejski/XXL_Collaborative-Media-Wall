import React, { useState } from "react";
import styles from "./SharedData.module.css";

const SharedData = (props) => {
  return <div className={styles.SharedData}>Movies and photos</div>;
};

export default SharedData;
